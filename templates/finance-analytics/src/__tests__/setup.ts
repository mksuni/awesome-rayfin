// Vitest global setup. Runs once before the suite in the jsdom environment.
// `@testing-library/jest-dom` adds DOM-aware matchers (toBeInTheDocument,
// toHaveFocus, …) used by the component render tests.
import "@testing-library/jest-dom/vitest";
